/////////////////
// IN DEVELOPMENT
// Cart keeps an array of the menu items that have been added to cart

$().ready(() => {
  // This selects all the add to cart buttons across the menu item cards
  $('button.add-to-cart');
});
